import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  titleFooter: {
    fontSize: "1em",
    color: "#333",
    textAlign: "center",
  }
});

export default useStyles;
